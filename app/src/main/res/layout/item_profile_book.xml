<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/size_6dp"
    app:cardCornerRadius="@dimen/space_x1">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/space_x1_half">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/iv_item_profile_book_picture"
            android:layout_width="match_parent"
            android:layout_height="@dimen/profile_picture_height"
            android:scaleType="centerCrop"
            app:layout_constraintTop_toTopOf="parent"
            app:shapeAppearanceOverlay="@style/CommonRoundedImageView"
            tools:src="@drawable/ic_launcher_background" />

        <TextView
            android:id="@+id/tv_item_profile_book_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_x2"
            android:gravity="center"
            android:textColor="@color/text_color_primary"
            android:textSize="@dimen/text_size_h6"
            app:layout_constraintTop_toBottomOf="@id/iv_item_profile_book_picture"
            tools:text="M Moersalin" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
